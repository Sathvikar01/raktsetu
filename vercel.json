{
  "$schema": "https://openapi.sh/vercel.json",
  "buildCommand": "node scripts/db-switch.mjs postgres && prisma generate && node -e \"require('fs').rmSync('.next/cache',{recursive:true,force:true})\" && next build",
  "crons": [
    {
      "path": "/api/cron/outbox",
      "schedule": "0 3 * * *"
    },
    {
      "path": "/api/cron/emergency",
      "schedule": "0 4 * * *"
    }
  ]
}
